{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json', 'yarn.lock'].forEach((f) => { try { fs.unlinkSync(f); } catch {} }); const ua = process.env.npm_config_user_agent || ''; if (!ua.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api-server": "pnpm --filter ./artifacts/api-server run dev",
    "dev:pos-app": "pnpm --filter ./artifacts/pos-app run dev",
    "dev:local": "pnpm --parallel --workspace-concurrency Infinity --filter ./artifacts/api-server --filter ./artifacts/pos-app run dev",
    "build:pos-app": "pnpm --filter ./artifacts/pos-app run build",
    "serve:pos-app": "pnpm --filter ./artifacts/pos-app run serve"
  },
  "private": true,
  "devDependencies": {
    "@types/connect-pg-simple": "^7.0.3",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "passport-google-oauth20": "^2.0.0"
  }
}
