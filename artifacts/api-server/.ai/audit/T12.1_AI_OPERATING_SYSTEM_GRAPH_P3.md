# T12.1 — AI Operating System Wiring Graph (Part 3)

---

# SECTION 10 — Wiring Validation

## Full Component Table

| Component | Incoming | Outgoing | Owner | Status |
|-----------|----------|----------|-------|--------|
| `routes/ai.ts` | HTTP POST | `applicationRuntime.executeMessage()` | API Layer | ✓ CONNECTED |
| `application-runtime-adapter` | `routes/ai.ts`, `mission-bg-engine`, `proposal-executor` | `ceoRuntime.execute()`, `ctoProgram.execute()`, etc. (8) | App Layer | ✓ CONNECTED |
| `ceoRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `ctoProgram.execute()` | `app-runtime-adapter` | `callDeepSeekWithTools()` | Executive | ✓ CONNECTED |
| `cooRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `cfoRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `cmoRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `caioRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `chroRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `ckoRuntime.execute()` | `app-runtime-adapter` | `callDeepSeek()` | Executive | ✓ CONNECTED |
| `ExecutiveDispatchRegistry` | EIOS stage, exec-collaboration | `handler.decide(brief, {})` | EIOS | ✓ CONNECTED |
| `PipelineEngine` | TriggerEngine | 11 stages | EIOS | ✓ CONNECTED |
| `MissionBackgroundEngine` | schedule | `applicationRuntime.executeMessage()` | Mission | ✓ CONNECTED |
| `ProposalExecutor` | proposal event | `applicationRuntime.executeMessage()` | Program | ✓ CONNECTED |
| `ExecutiveCollaboration` | mission | `ExecutiveDispatchRegistry.dispatch()` | Org | ✓ CONNECTED |
| `callDeepSeek` / `callDeepSeekWithTools` | All execs | DeepSeek HTTP API | LLM | ✓ CONNECTED |
| `executeToolCall` | Execs (CTO, CAIO, CHRO) | Filesystem/Git/SSH/DB | Tools | ✓ CONNECTED |
| **RICAdapter.assemble()** | **NOTHING** | **RuntimeIntelligenceCore** | **RIC** | **⚠️ ORPHAN** |
| **RuntimeIntelligenceCore** | **RICAdapter** | **All RIC stages** | **RIC** | **⚠️ ORPHAN** |
| **UnifiedAwarenessEngine** | **RIC** | **UnderstandingEngine** | **RIC** | **⚠️ ORPHAN** |
| **UnderstandingEngine** | **RIC** | **RetrievalPlanner** | **RIC** | **⚠️ ORPHAN** |
| **RetrievalPlanner** | **RIC** | **GroundingLayer** | **RIC** | **⚠️ ORPHAN** |
| **GroundingLayer** | **RIC** | **5 providers** | **RIC** | **⚠️ ORPHAN** |
| **VerificationEngine** | **RIC** | **ConfidenceAggregator** | **RIC** | **⚠️ ORPHAN** |
| **ConfidenceAggregator** | **RIC** | **ContextBuilder** | **RIC** | **⚠️ ORPHAN** |
| **RuntimeContextBuilder** | **RIC** | **ExecutiveContextAdapter** | **RIC** | **⚠️ ORPHAN** |
| **ExecutiveContextAdapter** | **RIC** | **NO ONE** | **RIC** | **⚠️ ORPHAN** |
| **MetricsStore** | **RIC** | **NO ONE** | **RIC** | **⚠️ ORPHAN** |
| **ReflectionEngine** | **RIC** | **NO ONE** | **RIC** | **⚠️ ORPHAN** |
| **PastPlanMemory** | **RIC** | **RetrievalPlanner** | **RIC** | **⚠️ ORPHAN** |
| **CircuitBreaker** | **RIC** | **GroundingLayer** | **RIC** | **⚠️ ORPHAN** |
| **RuntimeIntelligenceCompat** | **NO ONE** | **RICAdapter** | **Compat** | **⚠️ ORPHAN** |
| **CIO Executive** | Type definition only | — | — | **⚠️ MISSING** |

## Summary

| Status | Count | Components |
|--------|-------|-----------|
| ✓ CONNECTED | 18 | Routes, adapter, 8 execs, dispatch, pipeline, mission, tools, LLM |
| ⚠️ ORPHAN (no incoming) | 15 | Entire RIC module + compat layer |
| ⚠️ MISSING | 1 | CIO executive runtime |
| **TOTAL** | **34** | |

**Finding: 44% of the system (15 of 34 components) is dead code with no incoming connections.**

---

# SECTION 11 — Final Architecture

## Complete AI Operating System — Target Graph

```
┌──────────────────────────────────────────────────────────────────────────┐
│                              USER                                        │
│              (Web Chat / Mission UI / Dashboard / API Client)            │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │ POST /api/ai/chat
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                          API LAYER (routes/)                             │
│                                                                          │
│  routes/ai.ts: POST /chat → requireRole('owner'), SSE streaming         │
│  routes/ai.ts: GET /missions, POST /mission                              │
│  routes/dashboard.ts: GET /summary, /financial, /top-products            │
│  (93 other CRUD endpoints — no AI involvement)                          │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                     RUNTIME GATEWAY (target — NEW)                       │
│                                                                          │
│  RuntimeGateway.assemble(input)                                          │
│  ├── Resolve target executive from @mention or mode                     │
│  ├── Call RICAdapter.assemble() → RuntimeContext                        │
│  ├── Call ExecutiveContextAdapter.mapToExecutive() → ExecutiveContext   │
│  ├── Dispatch to target Executive.decide(executiveContext)              │
│  └── Build GatewayResult { runtimeContext, decision, metrics }          │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│               RUNTIME INTELLIGENCE CORE (EXISTS — to be activated)       │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    AWARENESS LAYER                                │   │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐  │   │
│  │  │ BusinessState│ │ DigitalTwin  │ │ Mission + Runtime + Health│  │   │
│  │  └──────┬───────┘ └──────┬───────┘ └──────────┬───────────────┘  │   │
│  │         └────────────────┼────────────────────┘                   │   │
│  │                    ▼                                               │   │
│  │          UnifiedAwarenessEngine.collect()                         │   │
│  │          → AwarenessSynthesizer.synthesize()                      │   │
│  │          → ContradictionDetector.detect()                         │   │
│  │          → AwarenessPrioritizer.prioritize()                      │   │
│  │          → AwarenessBrief                                         │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    COGNITIVE LAYER                                │   │
│  │                                                                  │   │
│  │  UnderstandingEngine.analyze(message, awarenessBrief)            │   │
│  │  → Intent, Domain, Entities, Risk, Confidence                    │   │
│  │  → Fallback: regex-based UnderstandingFallback                   │   │
│  │         │                                                         │   │
│  │         ▼                                                         │   │
│  │  RetrievalPlanner.plan(understanding, metadata, tools)           │   │
│  │  → LLM generates capability-based tasks                          │   │
│  │  → PastPlanMemory.findSimilar() → few-shot examples              │   │
│  │  → Fallback: PastPlanMemory or degradedPlan                      │   │
│  │         │                                                         │   │
│  │         ▼                                                         │   │
│  │  GroundingLayer.execute(plan)                                    │   │
│  │  → CircuitBreaker.check() → health check → execute               │   │
│  │  → 5 providers (parallel per dependency level)                   │   │
│  │  → Exponential backoff on retries                                │   │
│  │         │                                                         │   │
│  │         ▼                                                         │   │
│  │  VerificationEngine.verify(understanding, plan, grounding)       │   │
│  │  → 6 rules: domain, entity, file, tool, memory, operational      │   │
│  │  → Returns: VerificationResult with failed checks                │   │
│  │         │                                                         │   │
│  │    ┌────┴────┐                                                    │   │
│  │    ▼ if < 0.75                                                    │   │
│  │  REPLAN LOOP (max 2 iterations)                                  │   │
│  │  → Replan → Re-ground → Re-verify → Track RefinementEntry       │   │
│  │    │                                                               │   │
│  │    └── if >= 0.8: PastPlanMemory.store()                         │   │
│  │         │                                                         │   │
│  │         ▼                                                         │   │
│  │  ConfidenceAggregator.aggregate(...)                              │   │
│  │  → overall = reasoning × groundingScore × verification           │   │
│  │         │                                                         │   │
│  │         ▼                                                         │   │
│  │  RuntimeContextBuilder.build(ALL STAGE OUTPUTS)                   │   │
│  │  → Single immutable RuntimeContract                              │   │
│  │  → frozen via deepFreeze(structuredClone)                        │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    MAPPING LAYER                                  │   │
│  │  ExecutiveContextAdapter.mapToExecutive(runtimeContext)          │   │
│  │  → Extracts intent, domain, confidence, knowledge, memory,       │   │
│  │    operational, repository, awareness, refinement                │   │
│  │  → Returns: ExecutiveContext                                     │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    LEARNING LAYER                                 │   │
│  │  MetricsStore.recordRequest(domain, confidence, degraded, ...)   │   │
│  │  ReflectionEngine.reflect(confidence, verification, ...)         │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                       EXECUTIVE RUNTIME                                  │
│                                                                          │
│  CEO.decide(executiveContext)  │  CTO.decide(executiveContext)          │
│  COO.decide(executiveContext)  │  CFO.decide(executiveContext)          │
│  CMO.decide(executiveContext)  │  CAIO.decide(executiveContext)         │
│  CHRO.decide(executiveContext) │  CKO.decide(executiveContext)          │
│                                                                          │
│  Each executive receives:                                                │
│  { intent, domain, confidence, knowledge, memory, operational,           │
│    repository, awareness, refinement }                                   │
│  NO LONGER builds own context, selects tools, queries memory             │
│                                                                          │
│  Returns: ExecutiveDecision { role, action, reasoning, confidence }     │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      EXECUTION LAYER                                     │
│                                                                          │
│  Tool Adapter (ai/tools/tool-adapter.ts)                                │
│  → executeToolCall(name, args)                                          │
│  → Filesystem: readLocalFile, writeLocalFile, searchRepoFiles           │
│  → GitHub: fetchGitHubFile                                              │
│  → SSH: sshExec                                                         │
│  → Database: orders, expenses, inventory, shift queries                 │
│                                                                          │
│  LLM Adapter (ai/llm/llm-adapter.ts)                                    │
│  → callDeepSeek(systemPrompt, messages, tools)                          │
│  → callDeepSeekWithTools(systemPrompt, messages, tools)                 │
│  → callLLMWithTools(prompt, tools)                                      │
│  → DeepSeek HTTP API (POST /chat/completions)                           │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    EIOS RUNTIME                                          │
│                                                                          │
│  PipelineEngine (internal/PipelineEngine.ts)                            │
│  → Sequential execution of pipeline stages                              │
│  → CircuitBreaker + Bulkhead + Timeout per stage                        │
│  → PipelineGraphRegistry.getExecutionOrder() → topological sort         │
│                                                                          │
│  Pipeline Stages (12 total with ric_assemble):                          │
│  event_valid → business_intel → decision_context → decision_engine      │
│  → north_star → strategy_sim → strategy_engine → exec_planner           │
│  → workflow_runtime → brief_generator → ric_assemble → exec_runtime     │
│                                                                          │
│  ExecutiveDispatchRegistry.dispatch(role, runtimeContext)               │
│  → CEO | CTO | CFO | COO | CMO | CAIO | CHRO | CKO                     │
│                                                                          │
│  Mission Engine (ai/runtime/mission-engine.ts)                          │
│  → Creates missions, tracks progress, records history                   │
│                                                                          │
│  Governance (eios-runtime/internal/runtime-governance/)                 │
│  → Security policies, compliance, audit trail                           │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      FEEDBACK LOOPS                                      │
│                                                                          │
│  LOOP 1: Intra-request Refinement                                       │
│  Verification → (<0.75) → Replan → Re-ground → Re-verify → Context      │
│                                                                          │
│  LOOP 2: Cross-request Learning                                         │
│  Reflection → MetricsStore → PastPlanMemory → Next Request Planning      │
│                                                                          │
│  LOOP 3: System-level Adaptation                                        │
│  HealthMonitor → CircuitBreaker → AwarenessBrief → Adaptive Routing      │
│                                                                          │
│  LOOP 4: EIOS Pipeline (external triggers)                              │
│  TriggerEngine → PipelineResolver → PipelineEngine → 12 stages → Result │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                         RESPONSE                                        │
│                                                                          │
│  GatewayResult = {                                                      │
│    runtimeContext: RuntimeContext,  (full immutable contract)           │
│    executiveDecision: ExecutiveDecision,  (executive's output)          │
│    confidence: number,  (overall system confidence)                     │
│    executionTimeMs: number,                                              │
│    stages: string[],  (pipeline stages executed)                        │
│    awareness: { situation, health, businessRisk }                       │
│  }                                                                       │
│                                                                          │
│  → SSE streamed to client via routes/ai.ts                              │
│  → Logged to diagnostics                                                │
│  → Recorded in knowledge (if mission-related)                           │
└──────────────────────────────────────────────────────────────────────────┘
```

## Key Metrics — Target Architecture

| Metric | Value |
|--------|-------|
| **Entry points** | 1 (RuntimeGateway) |
| **Executive invocations** | 1 per request (not 8) |
| **LLM calls per request** | 3 (Understanding + Planning + Executive) |
| **LLM calls with replan** | 3-5 (if replan loop triggers) |
| **Context types** | 1 (RuntimeContext) |
| **Executive input** | 1 (ExecutiveContext, from RuntimeContext) |
| **Verification** | 6 rules before executive decision |
| **Awareness** | 7 sources, collected per request |
| **Learning** | Per-request metrics + reflection |
| **Dead code** | 0% (all components activated) |

## Gap-to-Target Summary

| # | What's Missing | Why It's Missing | Effort to Fix |
|---|---------------|------------------|---------------|
| 1 | RuntimeGateway class | Never built | 2 weeks |
| 2 | Routes call RuntimeGateway | Routes call old adapter | 1 day |
| 3 | RIC has incoming edge | No caller invokes RIC | 1 day (via Gateway) |
| 4 | Executives receive RuntimeContext | Executives have custom types | 2-3 weeks (all 8) |
| 5 | Executives stop building own context | Each builds inline | 1-2 weeks gradual |
| 6 | EIOS `ric_assemble` stage | Stage not defined | 1 week |
| 7 | EIOS brief_generator uses RuntimeContext | Uses ExecutiveBrief | 1 week |
| 8 | ExecutiveDispatchRegistry passes context | Passes `{}` empty | 1 day |
| 9 | 9 stub stages removed | Not implemented | 1 week |
| 10 | CIO runtime implemented | Type-only, no implementation | New project (T13) |

**TOTAL ESTIMATED EFFORT: 6-8 weeks (as detailed in T12_MIGRATION_BLUEPRINT.md)**
